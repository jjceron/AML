# Ignore everything by default
*

# Always keep this file
!.gitignore

# Allow project folder
!project/

# Allow subfolders explicitly so exceptions inside them work
!project/build/
!project/config/

# Keep the root notebook
!01.ipynb

# Keep the compiled PDF
!project/build/main.pdf

# Keep the custom LaTeX class
!project/config/report_type.cls

# Keep all .tex files in content/
!project/content/*.tex

# Keep all .tex files in tikz/
!project/tikz/*.tex 
